# text-intelligence configuration for the local environment.
# Non-secret values only. The LLM provider's API key (when not using the mock
# provider) is read from the environment, never from this file.
app_name: text-intelligence
api_prefix: /text-intelligence/api/v1

provider:
  name: mock
  model: mock-1
  timeout_seconds: 30
